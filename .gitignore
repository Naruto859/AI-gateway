# secrets & local state — never commit
data.db
data.db-*
.bak/
proxies.txt
.env
*.log
__pycache__/
*.pyc
.venv/
venv/
preview/

data/
# the sandbox symlinks `data` at the DB dir; match the symlink itself, not just a dir
data
